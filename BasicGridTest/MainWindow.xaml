<Window x:Class="BasicGridTest.MainWindow"
                xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                xmlns:local="clr-namespace:BasicGridTest"
                xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
                Title="MainWindow" Height="350" Width="525">
    <Window.Resources>
        <local:MyViewModel x:Key="MyViewModel" />
    </Window.Resources>
    <Grid DataContext="{StaticResource MyViewModel}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <telerik:RadGridView Name="grd" ItemsSource="{Binding Main}"
                                 AutoGenerateColumns="True" 
                                 CanUserInsertRows="True" NewRowPosition="Top" GroupRenderMode="Flat"  RowEditEnded="grd_RowEditEnded" AddingNewDataItem="grd_AddingNewDataItem"/>

        <Button Name="Button1"
		        Grid.Row="1"
		        Margin="5"
		        HorizontalAlignment="Stretch"
		        Click="Button1_Click"
		        Content="Add a Club" />
    </Grid>
</Window>
