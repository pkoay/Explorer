<base:RHSTabGridViewBase_ReadOnly
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:base="clr-namespace:WalzExplorer.Controls.RHSTabs"
             xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
             
             xmlns:WalzExplorer="clr-namespace:WalzExplorer"
             xmlns:tab="clr-namespace:WalzExplorer.Controls.RHSTabs"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" d:DesignWidth="402.09" d:DesignHeight="143.881"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"  mc:Ignorable="d"
             x:Class="WalzExplorer.Controls.RHSTabs.Project.SummaryView">
    <UserControl.Resources>
      
        <!--<DataTemplate x:Key="ToolTipTemplate">
            <TextBlock Text="ToolTip!!"/>
        </DataTemplate>-->

    </UserControl.Resources>
    <Grid>
        <!--<Grid.Resources>
            <Style TargetType="telerik:GridViewHeaderCell" x:Key="GridViewHeaderCellToolTip">
                <Setter Property="ToolTipService.ToolTip" Value="My header tooltip"/>
            </Style>
            <DataTemplate x:Key="ToolTipTemplate">
                <TextBlock Text="{Binding Name}"/>
            </DataTemplate>
        </Grid.Resources>-->
        <telerik:RadGridView  x:Name="grd" AutoGeneratingColumn="grd_AutoGeneratingColumn"/>
    </Grid>
</base:RHSTabGridViewBase_ReadOnly>
